import { Box } from "@mantine/core";

/**
 * The app title bar UI component.
 */
export default function TitleBar() {
  return (
    <Box
      className="app-drag"
      bg="gray.1"
      h="2rem"
      style={{
        alignItems: 'center'
      }}
    >
    </Box>
  )
}
